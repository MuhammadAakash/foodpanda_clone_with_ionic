import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'foodpanda_clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
